# === Security: secrets & credentials (fleet standard) ===
.env
.env.*
!.env.example
!.env.template
*.key
*.pem
*.p12
*.pfx
credentials*
secrets/
*.secrets
.secrets.map
*_rsa
*_ed25519
*.gpg

# Kin semantic VCS local repo state — not for git
.kin/
# Kin MCP config backups
.mcp.json.bak.kincap-*
